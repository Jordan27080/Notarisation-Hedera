# .NET
bin/
obj/
*.db
*.db-shm
*.db-wal
appsettings.Development.json
# appsettings.json volontairement tracké avec des placeholders.
# Mettez vos vraies valeurs dans appsettings.Development.json (gitignored).

# Node
node_modules/
dist/
.env
# Le lockfile est généré localement (versions npm différentes selon les machines)
# Les versions sont déjà fixées dans package.json
package-lock.json

# IDE
.vs/
.vscode/
*.user
